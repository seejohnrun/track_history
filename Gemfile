source :rubygems

require File.expand_path('../lib/track_history/version', __FILE__)

gem 'track_history', TrackHistory::VERSION, :path => File.expand_path('..', __FILE__)

group :test do
  gem 'rspec', '~> 1.3'
  gem 'mysql'
end

