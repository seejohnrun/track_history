* has_many serialization
* convenient generators that make nice indexes for people
* allow other than ActiveRecord

------------------
* separate need for inferrence to have before && after
* option to disable inferring of fields to track
* track all used historical fields and raise conflicts
* before and after should be able to be strings as specified to #field
* make the enum field nameable
* ability to enable and disable the action enum
* make created_at optional
