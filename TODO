* has_many serialization
* convenient generators that make nice indexes for people
* allow other than ActiveRecord

needed to announce
------------------
* separate modules into separate files and autoload them
* separate inference before / after need
* option to disable inference
* track all used historical fields and report conflicts via 'raise'
* change away from Hash and towards other classes
* before and after should be able to be strings
* if you specify just one of before / after - you should not save the other
* make the enum field nameable
* ability to enable and disable the action enum
